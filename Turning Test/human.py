def human_reply_stub(user_msg: str) -> str:
    """
    Demo-only: simulates a human reply when no second real user is connected.
    If you want it to be a real human, build another UI and send messages from there.
    """
    return f"(human) ok ok... you said: '{user_msg}'"
