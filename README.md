# AI Programming Assignments (Deadline: 5 March 2026)

**Name:** Shaon Ryan Ranjit  
**Roll No.:** SE24UCSE057  

This repository contains solutions for the AI programming assignments:

1. **Turing Test + CAPTCHA**
   - Designed an architecture explaining how a Turing Test system and a CAPTCHA system can be implemented.
   - Includes modular breakdown (client/UI, server, session manager, verification, logging, storage).

2. **Simple Reflex Agent for AQI**
   - Built a simple reflex agent that reads environmental sensor parameters from a file (dataset) and outputs the AQI category for the region.
   - Uses rule-based mapping from input parameters to AQI bands.

3. **Uninformed Search (BFS, DFS) on Water Jug Problem**
   - Implemented BFS and DFS strategies for the Milk/Water Jug problem.
   - Compared their performance using:
     - steps in solution
     - nodes expanded
     - runtime (optional)
   - BFS guarantees shortest path (minimum moves), DFS may find a longer path depending on traversal order.

